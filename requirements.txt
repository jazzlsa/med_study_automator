# Configuração e CLI
pydantic>=2.7.0
pydantic-settings>=2.2.0
pyyaml>=6.0.1
python-dotenv>=1.0.1
click>=8.1.7
loguru>=0.7.2
rich>=13.7.0
streamlit>=1.32.0

# IA e Multimodal
google-genai>=0.1.1
pymupdf>=1.24.0
pillow>=10.2.0

# Reparo de JSON malformado retornado pelo Gemini - sem response_schema
# estruturado, o modelo às vezes esquece de escapar aspas literais dentro de
# valores string (ex.: transcrição de alguém lendo uma citação em voz alta),
# o que quebra json.loads mesmo com strict=False. Caso real visto em produção
# (core/multimodal_processor.py, _parse_json_response).
json_repair>=0.25.0

# Anki e Integrações
requests>=2.31.0
genanki>=0.13.1

# Google Sheets & Drive
gspread>=6.0.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.120.0
gdown>=5.1.0

# Cloud Run Jobs: persistência do SQLite entre execuções via GCS (só é usada de
# fato quando a env var GCS_DB_BUCKET está configurada - ver database/db.py)
google-cloud-storage>=2.14.0

# NotebookLM (CLI notebooklm-py). No Windows local, instale com o extra [browser]
# também (pip install "notebooklm-py[browser,headless]") pra poder rodar
# `notebooklm login` interativo. O Dockerfile do Cloud Run instala só
# [headless,impersonate] - sem Playwright/Chromium, que não é necessário no dia a
# dia (só no bootstrap do master-token, feito no Windows, nunca no container).
notebooklm-py>=0.8.1

# Testes
pytest>=8.0.0
pytest-asyncio>=0.23.0